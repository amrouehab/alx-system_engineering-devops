#!/bin/bash

# Count directories using find with -type d (directory) and grep to filter out . and ..
num_dirs=$(find . -type d ! -name "." ! -name ".." | grep -c ^d)

echo "There are $num_dirs directories (including hidden) in the current directory."

